export interface EcomInter {
    vendorId: number;
    firstName: string;
    lastName: string;
    contact: string;
    email: string;
    address: string;
    stateId: number;
    cityId: number;
    stateName?: string; // Optional field if needed
    cityName?: string; 
  }
  
